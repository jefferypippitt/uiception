# Optional: Chrome Origin Trial so Liquid warps HTML without chrome://flags.
# Register once for your production origin (check "match all subdomains" if needed):
# https://developer.chrome.com/origintrials → html-in-canvas
# Example: Web Origin = https://yoursite.com
NEXT_PUBLIC_HTML_IN_CANVAS_OT_TOKEN=
